该代码库以uv为包管理器，运行和测试代码用 `uv run` 形式，前端代码调试运行则不必使用这种形式，仅仅针对python
虚拟环境在文件夹 venv
技术文档可放到 /docs/

## 组件颗粒度及维护原则：
- 合理提取公共组件，使其可适当复用，既不过度工程化，也不要代码冗余，模块拆分符合高解耦的特性。
- 涉及到组件创建、修改、删除，在完成后均记录到 /docs/component.md，保持对该文档的维护更新。