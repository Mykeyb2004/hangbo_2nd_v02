# 动态满意度研究报告开发方案

## 1. 目标与背景

本方案面向“基于原始问卷数据动态生成满意度研究报告”的实现工作，目标不是写死一份报告内容，而是建立一套可以复用的报告生成机制。该机制应能够读取原始问卷数据、字段映射和报告模板定义，自动完成统计、分群、图表规划与 Markdown 报告装配。

本次场景的核心难点不在基础统计，而在以下三点：

- 同时支持 `全体总体`、`分客群对比`、`客群专属体验` 三类展示逻辑。
- 避免把不同适用范围的分支题混入同一“总体”口径。
- 在客群数量、样本量、有效字段结构发生变化时，仍能稳定生成报告。

## 2. 设计原则

### 2.1 规则驱动

- 报告结构由模板定义。
- 字段是否参与分析由映射文件定义。
- 章节如何出图、是否分群、是否仅限适用样本，由规则引擎决定。

### 2.2 数据与内容解耦

- 原始数据负责提供样本事实。
- 字段映射负责提供题目语义、题型、适用范围和跳过规则。
- 报告模板负责定义章节、展示策略与文案骨架。

### 2.3 不写死统计结果

- 模板中不写死客群数量、具体占比、具体均值。
- 模板只声明“如何生成”而不是“生成什么数值”。

### 2.4 先保证口径正确，再考虑展示丰富

- 分支题默认不做全样本总体。
- 小样本客群默认不单列展示。
- 开放题默认只做文本归纳，不进入主量化图。

## 3. 范围界定

### 3.1 本期范围

- 读取 Excel 原始数据并标准化。
- 读取 `survey_field_mapping.csv` 并构建字段语义层。
- 按报告模板动态生成章节数据上下文。
- 输出 Markdown 报告草稿。
- 输出图表清单与图表所需的数据摘要。

### 3.2 暂不纳入本期

- 复杂自然语言自动写作优化。
- 多版本排版导出（如 PPT、Word）。
- 高级文本挖掘与情感分析。
- 全自动可视化出图渲染。

## 4. 输入与输出

### 4.1 输入

- 原始数据文件：`data/data.xlsx`
- 字段映射文件：`outputs/survey_field_mapping.csv`
- 问卷结构说明：`问卷.md`
- 报告模板文件：建议新增 `configs/report_template.yaml`
- 运行配置：建议新增 `configs/report_rules.yaml`

### 4.2 输出

- 研究报告 Markdown：如 `outputs/generated_report.md`
- 报告结构化中间结果：如 `outputs/generated_report_context.json`
- 图表清单：如 `outputs/chart_plan.json`
- 统计摘要：如 `outputs/metrics_summary.json`

## 5. 核心业务规则

### 5.1 字段分层规则

基于字段映射中的 `analysis_status`、`field_type`、`applicable_to`，将字段分为以下四类：

- `全体共性字段`
  条件：`analysis_status=analyze` 且 `applicable_to` 为空
- `客群专属字段`
  条件：`analysis_status=analyze` 且 `applicable_to` 非空
- `文本归纳字段`
  条件：`analysis_status=qualitative_review`
- `跳过字段`
  条件：`analysis_status=skip`

### 5.2 报告展示规则

- `全体共性体验`
  只使用全体共性字段，先展示全体总体。
- `客群共性对比`
  只对全体共性字段按客群分组统计。
- `客群专属体验`
  只在专属字段对应的适用样本内部统计，不参与全体总体。
- `文本意见归纳`
  只使用文本归纳字段，单独汇总。

### 5.3 客群识别与展示规则

- 客群主键默认来自 `受访身份`。
- 系统运行时动态识别有效客群及样本量。
- 通过阈值决定客群是否单列展示：
  - `min_group_n_for_chart`
  - `min_group_n_for_breakdown`
- 小样本客群处理方式由配置决定：
  - 合并到 `其他`
  - 只在脚注说明
  - 完全不出图但保留在数据摘要中

### 5.4 分支题展示规则

- 若字段存在 `applicable_to`，则只对该适用人群的数据求均值、分布和排序。
- 分支题不参与第四章“全体共性体验总览”。
- 若适用样本量不足，则输出“样本不足，不展开分析”。
- 若专属题之间语义相近，可选做并列展示，但必须标记“仅作方向性参考”。

### 5.5 评分题清洗规则

- 10 分制题目中仅保留 `1-10` 的值。
- `0`、空值、`11` 及其他非 `1-10` 值统一按空值处理。
- 清洗规则必须写入统计元数据，保证可追溯。

### 5.6 多选题与开放题规则

- 多选题采用选中率统计。
- “其他”补充项与开放题进入文本归纳流程。
- 文本归纳默认产出“高频主题 + 典型原话候选”，不直接给定最终结论。

## 6. 目标系统架构

建议采用“数据层 - 语义层 - 规则层 - 渲染层”四层架构。

### 6.1 数据层

职责：
- 读取原始 Excel 数据
- 完成列名标准化、空值处理、样本过滤
- 输出标准化答卷表

建议模块：
- `workbook_loader`
- `data_normalizer`
- `value_cleaner`

### 6.2 语义层

职责：
- 读取字段映射文件
- 为每个字段附加题型、标题、适用范围、分析状态等语义信息
- 形成“字段字典”

建议模块：
- `field_mapping_loader`
- `field_registry`

### 6.3 规则层

职责：
- 判断哪些字段进入哪些章节
- 判断哪些客群可以单列展示
- 判断是否出总体、对比图、专属题章节

建议模块：
- `audience_engine`
- `chapter_rule_engine`
- `metric_engine`
- `chart_planner`

### 6.4 渲染层

职责：
- 将章节数据上下文填充进 Markdown 模板
- 输出报告正文、图表占位和说明文字

建议模块：
- `context_builder`
- `markdown_renderer`
- `report_assembler`

## 7. 配置化设计

建议用 YAML 或 JSON 管理模板与规则，不将这些逻辑散落在代码里。

### 7.1 报告模板配置

建议新增：`configs/report_template.yaml`

建议包含：
- 章节顺序
- 每章标题
- 每章使用的字段选择器
- 每章分析模式
- 每章图表类型建议
- 每章正文模板片段

示例结构：

```yaml
chapters:
  - id: overall_common
    title: 全体共性体验总览
    mode: common_overall
    field_selector:
      analysis_status: analyze
      applicable_to: ""
      field_types: [rating_10pt, single_choice, multiple_choice_option]
    charts:
      - type: mean_ranking
      - type: distribution_bar
```

### 7.2 规则配置

建议新增：`configs/report_rules.yaml`

建议包含：
- 客群字段名
- 最小样本量阈值
- 小样本处理策略
- 分支题对照模式
- 文本归纳开关

示例结构：

```yaml
audience_field: 受访身份
min_group_n_for_chart: 10
min_group_n_for_breakdown: 15
small_group_strategy: footnote
exclusive_items_compare_mode: directional_only
text_summary_enabled: true
```

## 8. 统计与章节生成流程

### 8.1 步骤 1：读取与清洗数据

- 读取 Excel
- 应用评分题清洗规则
- 识别空值与异常值
- 形成统一数据表

### 8.2 步骤 2：构建字段语义索引

- 读取字段映射
- 识别全体共性字段、专属字段、文本字段、跳过字段
- 建立字段到章节主题的映射关系

### 8.3 步骤 3：识别客群与可展示范围

- 动态统计各客群样本量
- 应用阈值，生成“可单列客群列表”
- 同时识别每类分支题的适用样本量

### 8.4 步骤 4：生成章节统计上下文

- 对共性字段生成全体总体统计
- 对共性字段生成分客群对比统计
- 对专属字段生成适用样本内部统计
- 对文本字段生成归纳输入材料

### 8.5 步骤 5：生成图表计划

- 根据章节模式和字段类型自动推荐图表类型
- 形成图表清单与图表数据结构

### 8.6 步骤 6：渲染 Markdown 报告

- 将章节数据和模板片段合并
- 生成带占位图名、分析摘要和说明的 Markdown 报告

## 9. 建议代码结构

建议新增如下目录：

```text
src/
  reporting/
    __init__.py
    loaders/
      workbook_loader.py
      field_mapping_loader.py
    models/
      field_meta.py
      chapter_context.py
      metric_result.py
    engines/
      data_normalizer.py
      audience_engine.py
      metric_engine.py
      chapter_rule_engine.py
      chart_planner.py
      text_summary_engine.py
    renderers/
      markdown_renderer.py
      report_assembler.py
    cli/
      generate_report.py
configs/
  report_template.yaml
  report_rules.yaml
```

## 10. 关键数据契约

### 10.1 字段元信息对象

至少应包含：
- `column`
- `chart_title`
- `field_type`
- `analysis_status`
- `applicable_to`
- `question_group`
- `notes`

### 10.2 章节上下文对象

至少应包含：
- `chapter_id`
- `chapter_title`
- `mode`
- `eligible_groups`
- `fields`
- `metrics`
- `chart_specs`
- `narrative_inputs`

### 10.3 指标结果对象

至少应包含：
- `field`
- `population_scope`
- `group`
- `n`
- `mean`
- `distribution`
- `selection_rate`
- `missing_count`
- `notes`

## 11. 分阶段实施计划

### Phase A：模板与规则基座

目标：
- 完成报告模板配置和规则配置设计
- 搭好字段语义与章节规则的骨架

交付物：
- `report_template.yaml`
- `report_rules.yaml`
- 字段与章节选择器原型

### Phase B：统计引擎

目标：
- 完成全体总体、分群对比、专属题统计逻辑
- 完成评分题、多选题、单选题的指标计算

交付物：
- `metric_engine`
- `audience_engine`
- 中间结果 JSON

### Phase C：报告装配

目标：
- 完成 Markdown 章节渲染
- 完成图表清单生成
- 输出首版可读报告

交付物：
- `generated_report.md`
- `chart_plan.json`

### Phase D：验证与优化

目标：
- 用当前杭博二期数据验证规则正确性
- 优化小样本处理、文本归纳和章节文案

交付物：
- 验证样例报告
- 规则修订清单

## 12. 验收标准

- 同一套模板在不修改代码的前提下，可适配同结构问卷的新数据。
- 全体共性题、客群共性对比、客群专属题三类章节的口径不混乱。
- 小样本客群不会被错误单列。
- 分支题不会被纳入全体总体。
- 评分题异常值处理可追溯。
- 最终输出报告结构完整、章节清晰、图表规划可执行。

## 13. 风险与应对

### 13.1 客群样本过小

风险：
- 分群图失真或解释风险高。

应对：
- 配置最小样本量阈值。
- 小样本只保留说明，不强制出图。

### 13.2 问卷结构迭代

风险：
- 题目增减导致章节选择规则失效。

应对：
- 尽量依赖字段映射中的语义字段，而不是依赖固定列号。

### 13.3 文本题自动归纳质量不稳定

风险：
- 开放题摘要可能不稳定。

应对：
- 首期只产出文本素材与高频主题候选，不自动输出强结论。

### 13.4 章节模板过于刚性

风险：
- 新问卷结构变化时模板复用性下降。

应对：
- 将章节规则和图表计划抽象成可配置项。

## 14. 当前仓库的下一步落地建议

建议按如下顺序实施：

1. 新增 `configs/report_template.yaml` 与 `configs/report_rules.yaml`
2. 新增 `src/reporting/` 基础目录
3. 先实现“字段映射读取 + 客群识别 + 章节选择器”
4. 再实现“统计引擎 + Markdown 报告装配”
5. 最后补“图表计划输出 + 文本归纳接口”

## 15. 结论

当前最合理的实现路径不是直接写一份静态报告，而是建立一个“字段映射驱动 + 章节模板驱动 + 规则引擎驱动”的报告生成器。这样既能处理全体总体与分群对比，也能妥善处理不同客群的专属体验题，并为后续扩展到更多批次数据或类似问卷打下稳定基础。
